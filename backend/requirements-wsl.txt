# WSL / Linux requirements for GH05T3 backend (Python 3.12, unpinned)
# Install: pip install -r requirements-wsl.txt

# Core web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
starlette>=0.36.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Async HTTP
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0
websockets>=12.0

# AI / LLM clients
openai>=1.30.0
litellm>=1.40.0

# Vector DB / memory
chromadb>=0.5.0

# Data
numpy>=1.26.0
pandas>=2.2.0
networkx>=3.0

# Auth / JWT
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0

# Utils
orjson>=3.9.0
rich>=13.7.0
typer>=0.12.0
tenacity>=8.2.0
PyYAML>=6.0.1
python-dateutil>=2.9.0
loguru>=0.7.2
click>=8.1.7
tqdm>=4.66.0

# MongoDB async client
motor>=3.3.0
pymongo>=4.7.0

# GitHub integration
PyGithub>=2.3.0

# Scheduler
APScheduler>=3.10.0

# JWT / API auth
cryptography>=42.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# MAP-Elites quality-diversity (KAIROS)
ribs>=0.7.0

# Optional — STT/TTS (comment out if not needed)
# faster-whisper>=1.0.0
# kokoro>=0.9.2

# Stripe payments
stripe>=9.0.0

# Prometheus metrics
prometheus-client>=0.20.0

# MCP server
mcp>=1.0.0

# Tailscale peer discovery
requests>=2.31.0

# Google AI
google-genai>=1.0.0
google-generativeai>=0.8.0
